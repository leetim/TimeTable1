unit Main;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, IBConnection, sqldb, memds, db, FileUtil, Forms, Controls,
  Graphics, Dialogs, DbCtrls, Unit3, Menus;

type

  { TForm1 }

  TForm1 = class(TForm)
    DataSource1: TDataSource;
    IBConnection1: TIBConnection;
    MainMenu1: TMainMenu;
    FileItem: TMenuItem;
    AboutItem: TMenuItem;
    MenuItem3: TMenuItem;
    MenuItem4: TMenuItem;
    ExitItem: TMenuItem;
    SQLQuery1: TSQLQuery;
    SQLTransaction1: TSQLTransaction;
    procedure FormCreate(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R *.lfm}

{ TForm1 }

procedure TForm1.FormCreate(Sender: TObject);
begin
  IBConnection1.Connected := True;
  SQLQuery1.Active := True;
end;

end.

